<project>

    <!-- ... -->

    <plugins>
    
        <plugin>
            <groupId>org.mortbay.jetty</groupId>
            <artifactId>maven-jetty-plugin</artifactId>
            <version>6.1.10</version>
        </plugin>
        
    </plugins>
    
    <!-- ... -->
</project>

<!--
Then you can execute any of these commands:
    mvn jetty:run
    mvn jetty:run-war
    mvn jetty:run-exploded
 -->